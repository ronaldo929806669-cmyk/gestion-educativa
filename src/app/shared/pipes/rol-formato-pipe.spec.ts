import { RolFormatoPipe } from './rol-formato-pipe';

describe('RolFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new RolFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
